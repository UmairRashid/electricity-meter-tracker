# Database Configuration
DB_HOST=database
DB_PORT=5432
DB_NAME=meter_tracker
DB_USER=meter_user
DB_PASSWORD=CHANGE_ME_TO_SECURE_PASSWORD

# Backend Configuration
BACKEND_HOST=0.0.0.0
BACKEND_PORT=8000
CORS_ORIGINS=http://localhost:3000,http://192.168.*:3000,http://10.*:3000

# Frontend Configuration
VITE_API_URL=/api
FRONTEND_PORT=3000

# Optional: Set hostname for .local access (leave empty to disable)
# Example: HOSTNAME=my-meter-tracker
# This will enable access via http://my-meter-tracker.local:3000
HOSTNAME=

# Trusted hosts for security middleware (comma-separated)
# Add your local IP and hostname for network access
# Example: TRUSTED_HOSTS=192.168.1.100,my-computer.local
TRUSTED_HOSTS=

# Usage Tracking Configuration
MONTHLY_LIMIT_PER_METER=200
TOTAL_MONTHLY_LIMIT=600
DAYS_IN_MONTH=30

# Security Notes:
# - Only frontend port is exposed externally for security
# - Backend and Database communicate via internal Docker network
# - Use 192.168.* or 10.* patterns in CORS_ORIGINS for local network access
# - Change DB_PASSWORD to a strong password in production
# - Adjust usage limits based on your requirements